name: WarehousePilot CI/CD

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  backend-python-tests:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: WarehousePilot_app/backend

    steps:
    - uses: actions/checkout@v3
    - name: Set up Python
      uses: actions/setup-python@v3
      with:
        python-version: '3.10'
    
    - name: Install system dependencies
      run: |
        sudo apt-get update
        sudo apt-get install -y python3-dev libpq-dev
    
    - name: Install Python dependencies
      run: |
        python -m pip install --upgrade pip
        pip install wheel
        pip install -r requirements.txt
    
    - name: Run Python tests
      env:
        DATABASE_NAME: postgres

        DATABASE_USER: postgres.wctpncuvrrotnnzsrroc

        DATABASE_PASSWORD: BsuKwx5pjWPJ8E8d

        DATABASE_HOST: aws-0-us-east-1.pooler.supabase.com

        DATABASE_PORT: 6543
      run: |
        pytest tests/

  frontend-react-build:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: WarehousePilot_app/frontend

    steps:
    - uses: actions/checkout@v3
    - name: Use Node.js
      uses: actions/setup-node@v3
      with:
        node-version: '18.x'
    
    - name: Install frontend dependencies
      run: npm ci
    
    - name: Build React frontend
      run: npm run build
    
    - name: Run frontend tests
      run: echo "Skipping frontend tests" # Or npm test if tests are configured

  deploy:
    needs: [backend-python-tests, frontend-react-build]
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v3
    
    - name: Deploy to Production
      run: |
        echo "Deploying to production server"
        # Add your specific deployment commands
